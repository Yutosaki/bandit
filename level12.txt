bandit12@bandit:/tmp/ttt$ cat data8.bin
The password is FO5dwFsc0cbaIiH0h8J2eUks2vdTDwAn

まず色々試したりしたいので、言われている通りに/tmp配下にディレクトリを作った。その際は`mkdir`コマンドか、`mktemp -d`などで作る。
その後、中身を見ると圧縮されている(repeatedly compressed)。そのため、fileコマンドでそのファイルの形式を確認して、使えそうな解凍方法をひたすらmanを読んで調べて、適応する
困ったこととしてはsuffixが正しくないと怒られる時があり(gzipの時)、manをきちんと確認すると、`.gz`で終わっているファイルにのみ適応する、と書かれていたので、`mv`コマンドでファイル名の末尾に`.gz`って勝手につけたら上手く行った
見ての通り、合計8回解凍した
manを読むことがとにかく大事だった。オプションなどの説明もいつもなんとなくで使っている`tar -xvf`がそれぞれどんな意味なのかをきちんと把握した方が覚えやすいし、最悪その場で調べられる様にする
